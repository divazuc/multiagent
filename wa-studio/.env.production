# Production build config (Cloudflare Pages). Baked into the bundle at build time.
# Contains NO secrets — this is the public agent-server URL (Railway, custom domain).
VITE_AGENT_URL=https://wagent.divdev.co
