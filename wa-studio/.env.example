# Copy to .env.local and fill in values

# Supabase — anon/public key only (never the service role key)
VITE_SUPABASE_URL=https://mlbtqspcgdprmbytcsyc.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Agent server URL (production Railway URL — no trailing slash)
# Leave blank in local dev (Vite proxy handles it)
VITE_AGENT_URL=

# n8n base URL (legacy, keep for fallback)
VITE_N8N_URL=https://divazuc.app.n8n.cloud
